import 'package:flutter/material.dart';

class AdminDatasetScreen extends StatelessWidget {
  const AdminDatasetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Admin Dataset'),
      ),
    );
  }
}
